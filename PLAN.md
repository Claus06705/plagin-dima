📝 MASTER PLAN: plagin-dima (DeepSeek Android GUI) v0.2.0
Фаза 1: Фундамент и Настройки сборки (Kotlin/Gradle)

[ ] 1.1. Инициализация проекта через IntelliJ Platform Gradle Plugin (v2.2.1).

[ ] 1.2. Настройка build.gradle.kts: зависимости (OkHttp, Gson, SQLite-JDBC) и таргет на Android Studio Ladybug (242.*).

[ ] 1.3. Настройка plugin.xml: регистрация ToolWindow, добавление нашей иконки (deepseek-android.svg), регистрация базовых Actions и Services.

Фаза 2: Разработка AI-Bridge (Node.js)

[ ] 2.1. Создание структуры папки ai-bridge и package.json (Express, Axios, CORS).

[ ] 2.2. Реализация синхронного эндпоинта /api/chat для архитектурных вопросов.

[ ] 2.3. Реализация потокового эндпоинта /api/code (SSE) для генерации кода.

[ ] 2.4. Создание файла android/templates.js с системными промптами (Compose, MVVM, Room).

Фаза 3: Разработка Kotlin Backend (Ядро плагина)

[ ] 3.1. Реализация DeepSeekSettings и шифрования AES-256-GCM для безопасного хранения API-ключа DeepSeek.

[ ] 3.2. Написание DeepSeekService: HTTP-клиент (OkHttp) для связи с нашим локальным Node.js bridge.

[ ] 3.3. Создание AndroidContextCollector: парсинг AndroidManifest.xml и build.gradle (minSdk, зависимости, поиск Compose/ViewBinding).

[ ] 3.4. Реализация SessionService: подключение SQLite для локального хранения истории чатов (sessions.db).

Фаза 4: Разработка Frontend (React 19 + WebView2)

[ ] 4.1. Перенос структуры webview из проекта-донора, обновление зависимостей до React 19 и Fluent UI v9.

[ ] 4.2. Адаптация UI: удаление упоминаний Claude, синхронизация цветовой темы с Android Studio (Dark/Light).

[ ] 4.3. Реализация хука useDeepSeekStream для красивой отрисовки потокового ответа от сервера.

[ ] 4.4. Добавление поддержки слэш-команд в поле ввода (/activity, /compose, /migrate-compose).

Фаза 5: Интеграция фичей IDE (Связка Frontend + Kotlin)

[ ] 5.1. Настройка JS-Bridge: проброс сообщений из React в Kotlin и обратно.

[ ] 5.2. Реализация распознавания тегов файлов (@file) и ресурсов (@layout/, @drawable/) в чате.

[ ] 5.3. Интеграция IntelliJ Code Diff: кнопка во фронтенде, которая открывает нативное окно сравнения сгенерированного кода с текущим файлом.

[ ] 5.4. Реализация смарт-экшенов: автоматическая вставка сгенерированных Compose/XML файлов прямо в нужные директории проекта.

Фаза 6: Сборка и Тестирование

[ ] 6.1. Написание Unit-тестов для проверки сбора контекста и парсинга манифеста.

[ ] 6.2. Настройка скрипта сборки: компиляция Node.js скриптов, сборка Vite (React) и упаковка всего этого внутрь ZIP-архива плагина.
